#include<stdio.h>
int main()
{
    int ar[100],a,b,small,large;
    printf("array size = \n",a);
    scanf("%d",&a);
    printf("enter any number=\n");
    for(i=0,b<n;b++)
    {
        scanf("%d",&ar[b]);
    }
    large=ar[0];
    small=ar[0];
    for (i=1;b<a,b++)
    {
        if (ar[b]>large)
            large=ar[i];
        else if(a[i]<small)
        {
            small=a[i]
        }
        printf("large number = %d\n",large);
        printf("smallest number = %d\n",small);
        return 0;
    }
}
