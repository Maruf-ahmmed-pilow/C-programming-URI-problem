#include<stdio.h>
int main()
{
    char ch='W';
    if (ch>='S' && ch<'Z')
    {
        printf("%c is the upper character\n",ch);
    }
    else if (ch>='a' && ch<='z')
    {
        printf("%c is the fucker boy\n",ch);
    }
    return 0;
}
